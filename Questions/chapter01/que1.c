// Gross Salary is the amount of salary after adding all
// benefits and allowances but before deducting any tax
#include <stdio.h>

int main()
{
    // variable for getting basic salery is a
    int a;
    printf("Enter Ramesh's salery:");
    scanf("%d",&a);
    // variable for gross salery is b
    int b;
    formula for calculating gross salery
    b = a+(a*40/100)+(a*20/100);
    printf("%d", b);
    return 0;
}
