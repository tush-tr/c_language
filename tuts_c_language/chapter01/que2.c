// Calculate the area of a rectangle using inputs supplied from the user
#include<stdio.h>
int main(){
    int a,b;
    printf("Enter the height and width of the rectangle");
    scanf("%d%d",&a,&b);
    printf("the area of the rectangle = %d\n",a*b);
    return 0;
}