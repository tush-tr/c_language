#include<stdio.h>
#include<math.h> //adding the math library
int main(){
    double a,b;
    printf("Enter the value of height and width:");
    scanf("%lf%lf",&a,&b);
    // float c = pow(a,b);
    // printf("Area =%f",c);
    printf("Value of 4^5 is %f", pow(2,5));
    return 0;
}