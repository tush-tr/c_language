// check divisible by 97 or not
#include<stdio.h>
int main(){
    int n;
    printf("enter the number\n");
    scanf("%d",&n);
    // if n%97=0 then devisible else not
    printf("Divisiblity test returns (if value=0 then divisible else it's not): %d\n",n%97);
    return 0;

}